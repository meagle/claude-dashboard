// Re-exported from the agents registry (single source of truth for which processes count as
// known coding agents) so existing import paths (`../agentProcess`) keep working.
export { isKnownAgentProcessArgs } from './agents';
