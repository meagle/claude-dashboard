{
  "name": "claude-dashboard",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w packages/shared && npm run build -w packages/hook && npm run build -w packages/menubar",
    "test": "npm run test --workspaces --if-present",
    "dev": "npm run dev -w packages/menubar",
    "dev:hook": "npm run dev -w packages/hook"
  },
  "devDependencies": {
    "esbuild": "0.27.7",
    "react": "^19.2.5",
    "react-dom": "^19.2.5"
  }
}
